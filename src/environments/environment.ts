let host = "http://localhost";
let port = "50808";
//let url_host = "/api";
let url_host = "https://localhost:5001";
export const environment = {
  production: false,
  HOST_URL: url_host,
  PORT: port
};
